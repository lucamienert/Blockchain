#include "include/validator.h"

/**
 * @brief Validating new Block
 * 
 * @param block, block 
 * @return bool
 */
bool BC::isValidNewBlock(BC::Block block, BC::Block prevBlock)
{
    return true;
}

/**
 * @brief Validating the Chain
 * 
 * @param blockchain 
 * @return bool 
 */
bool BC::isValidChain(BC::Blockchain blockchain)
{
    return true;    
}